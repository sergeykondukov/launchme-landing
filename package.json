{
  "name": "launchme-landing",
  "private": true,
  "scripts": {
    "build": "pnpm run build:download-badges && pnpm run build:headers && pnpm run build:account && pnpm run build:paddle",
    "build:download-badges": "node scripts/build-download-badges.mjs",
    "build:headers": "node scripts/build-headers.mjs",
    "build:account": "node scripts/build-account.mjs",
    "build:paddle": "node scripts/build-paddle-config.mjs",
    "start": "serve .",
    "start:ipv4": "serve . -l tcp://127.0.0.1:3000"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.49.1"
  },
  "devDependencies": {
    "dotenv": "^16.4.7",
    "esbuild": "^0.25.0",
    "serve": "^14.2.4"
  }
}
